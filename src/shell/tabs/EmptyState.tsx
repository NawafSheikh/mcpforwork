/**
 * Board empty state. The first view a stranger sees, owned by src/onboarding (A12):
 * what this page is, what to do, and what will happen.
 */
import { Hero } from "../../onboarding";

export function EmptyState(): JSX.Element {
  return <Hero />;
}
